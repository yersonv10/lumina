---
title: Global CSS Variables
description: CSS Variable file to be used in globally
---

import { getFileContent } from "@/utils/get-file-content";

# Global CSS Variables

These are the design tokens `@lumina/ui/theme.css` (or `@lumina/ui/theme-dark.css`) adds to your project when you `@import` it — see [Quick Start](/get-started/quick-start). They're referenced across multiple core components, so reference this list if you want to override any of them.

<CodeBlockCard
  snippet={getFileContent("content/get-started/themes/default.css")}
  lang="css"
  fileName="light.css"
  showLineNumbers
/>

<CodeBlockCard
  snippet={getFileContent("content/get-started/themes/dark.css")}
  lang="css"
  fileName="dark.css"
  showLineNumbers
/>
